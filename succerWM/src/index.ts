import "./components/app-component"

const body = document.querySelector("body")//holt body
const appComponent = document.createElement("app-component")//holt unsere Komponente
body.appendChild(appComponent)//weist unsere Komponente dem Body zu